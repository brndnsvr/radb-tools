package cli

// This file defines command aliases for convenience.
// Aliases are configured directly in the command definitions (Use/Aliases fields).
//
// Common aliases:
// - route -> r, routes
// - contact -> c, contacts
// - snapshot -> snap, snapshots
// - history -> hist
// - search -> find
// - list -> ls
//
// These aliases are defined in their respective NewXxxCmd functions.
